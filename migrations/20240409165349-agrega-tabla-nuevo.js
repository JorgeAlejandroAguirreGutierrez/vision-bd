'use strict';

var dbm;
var type;
var seed;

/**
  * We receive the dbmigrate dependency from dbmigrate initially.
  * This enables us to not have to rely on NODE_PATH.
  */
exports.setup = function(options, seedLink) {
  dbm = options.dbmigrate;
  type = dbm.dataType;
  seed = seedLink;
};

exports.up = function(db) {
  console.log('Iniciando Modificacion........');
  try {
	  db.runSql(`
	    DO $$ DECLARE
	    BEGIN
      CREATE TABLE nuevo (
        id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
        codigo varchar,
        identificacion varchar,
        razon_social varchar,
        nombre_comercial varchar,
        genero varchar,
        apodo varchar,
        obligado varchar,
        direccion varchar,
        especial varchar,
        numero_resolucion_especial varchar,
        agente_retencion varchar,
        numero_agente_retencion varchar,
        regimen varchar,
        gran_contribuyente varchar,
        artesano_calificado varchar,
        facturacion_interna varchar,
        provincia varchar,
        canton varchar,
        parroquia varchar,
        celular varchar,
        correo varchar,
        establecimiento_sri varchar,
        estacion_sri varchar,
        contrasena_certificado varchar,
        contrasena_sri varchar,
        secuencial_factura_venta bigint,
        secuencial_factura_interna bigint,
        secuencial_retencion bigint,
        secuencial_guia_remision bigint,
        secuencial_nota_debito bigint,
        secuencial_nota_credito bigint,
        fecha_creacion	timestamp without time zone NOT NULL default CURRENT_TIMESTAMP, 
        fecha_actualizacion	timestamp without time zone NOT NULL default CURRENT_TIMESTAMP,
        PRIMARY KEY (id)
      );
	    END $$;
	  `);
	  return db.runSql('COMMIT');
   } catch (err) {
    console.error('Fail......');
    throw err;
  }
};

exports.down = function(db) {
  console.log('Iniciando Delete........');
  try {
	  db.runSql(`
	    DO $$ DECLARE
	    BEGIN
        DROP TABLE IF EXISTS nuevo;
	    END $$;
	  `);
	  return db.runSql('COMMIT');
   } catch (err) {
    console.error('Fail......');
    throw err;
  }
};

exports._meta = {
  "version": 1
};
